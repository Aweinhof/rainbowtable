# rainbowtable

To build prog : 
```bash
make
```

To clean build :
```bash
make clean
```

To run :
```bash
./rainbowtable [build/attack] [filename]
```

Fast compile and run : 
```bash
make clean && make && ./rainbowtable build
```
